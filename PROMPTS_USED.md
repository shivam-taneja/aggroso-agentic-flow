# Prompts Used During Development

Below is a log of the key prompts used to accelerate development.

## Backend Scaffolding
> "For this typeorm entity 'Workflow', Create a DTO that accepts an input string and an array of steps."

## Queue Implementation
> "I need to process these workflows in the background. Show me how to set up BullMQ in NestJS."

## Frontend Polling Logic
> "I am using TanStack Query (React Query). How can I implement a polling mechanism that fetches a specific endpoint every 2 seconds until the status is 'COMPLETED' or 'FAILED', and then stops polling?"

## Styling & UI
> "Using Tailwind CSS and Lucide React icons, give me a clean sidebar layout. It should be collapsible on mobile and fixed on desktop."